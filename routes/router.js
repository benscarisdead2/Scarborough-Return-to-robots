const express = require("express");
const router = express.Router();
const mongo = require("mongodb").MongoClient;
const databaseUrl = "mongodb://localhost:27017/robotsdb2";





module.exports = router;